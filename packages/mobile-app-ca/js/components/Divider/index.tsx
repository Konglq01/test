import React from 'react';
import { View } from 'react-native';
import { Styles } from './style';

const Divider: React.FC<undefined> = () => {
  return <View style={Styles.divider} />;
};

export default Divider;
