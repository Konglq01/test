#!/usr/bin/env bash

cd node_modules
tar -zcf aelf-sdk.tar.gz aelf-sdk


HASH=$(openssl sha256 aelf-sdk.tar.gz | awk '{print $2}')

echo ${HASH}

if [ "${HASH}" = "$AELF_SDK_NODE_MODULES_HASH" ]; then
  echo "aelf-sdk  version is OK"
else
  echo "aelf-sdk  version is wrong!"
  cd ..
  cd.......
fi