#!/usr/bin/env bash

cd node_modules/aelf-sdk/dist


HASH=$(openssl md5 aelf.cjs.js | awk '{print $2}')

echo ${HASH}

if [ "${HASH}" = "$AELF_SDK_NODE_MODULES_HASH" ]; then
  echo "aelf-sdk version is OK!"
else
  echo "aelf-sdk version is WRONG!"
  cd ..
  cd.......
fi