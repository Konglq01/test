{
  "name": "@portkey/constants",
  "publishConfig": {
    "access": "public"
  },
  "version": "0.0.1",
  "type": "commonjs",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "start": "tsc --watch",
    "handle-network": "cd ./scripts && node handleNetwork.js && cd .."
  },
  "dependencies": {
    "@portkey/types": "^0.0.1"
  }
}
