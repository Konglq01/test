import { StyleSheet } from 'react-native';
import { defaultColors } from 'assets/theme';

const { font3 } = defaultColors;

export const Styles = StyleSheet.create({
  divider: {
    height: 1,
    width: '100%',
    backgroundColor: font3,
  },
});
