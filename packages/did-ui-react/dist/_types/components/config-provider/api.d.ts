import { Verification } from '@portkey/api/api-did/verification/utils';
import { IStorage } from '@portkey/types/storage';
export declare let verification: Verification;
export declare const configVerification: (store: IStorage) => Verification;
